# 固定组合数值精修：R10第二次执行

2026年9月15日。完整沿用R10_METRIC_OBJECTIVE_PROTOCOL.md的源人口、四条件、主joint_future、epsilon0.001、学习器、日程、种子、匹配固定控制和外层评价定义。原协议SHA256为84c0eaf7faa6a6b31e19d7aa1cb0b759c1ce888ac6a456659aba6b6f889fdef4，原文件及第一次执行产物保留。

第一次执行完成60个Chronos模型后，在排除ett家族的mae_teacher固定凸组合处停止。SLSQP报告成功，但独立的一阶最优性差为1.1239498714e-7，高于原1e-7阈值，因此没有完成实验或读取最终验证评分。日志、旧程序和队列终态保存在metric-source-v001/failure_snapshot。

唯一计算变更是对未达到原最优性阈值的SLSQP结果追加数值精修。保持SLSQP的2,000步上限和ftol1e-13。每次从正权重中梯度最大的候选向梯度最小的候选转移权重，沿该可行方向用导数零点或边界求最优步长；至多200次，较小残差停止目标1e-8，最终放行阈值仍为1e-7。已满足原阈值的解完全不改。目标值不能在数值误差之外增加。此为同一凸优化问题的精度处理，不改变损失、平滑常数、样本或候选，也不放宽检验。

第二次执行使用metric-source-v002及metric-source-audit-v002，第一次执行的60个模型全部保留。独立审查须将这60个模型的参数、归一化统计、训练历史和身份信息与新执行逐项精确比较，确认数值精修没有改变学习器训练。新增模块只改变固定求解器；训练脚本、两种目标及其梯度的定义保持一致。其余冻结、完整结果保留、第一优先级及每小时检查要求沿用原协议。
