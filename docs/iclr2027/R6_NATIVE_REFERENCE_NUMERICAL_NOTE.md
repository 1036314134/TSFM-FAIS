# 训练扩展中的旧基线批次重放

2026-09-14，native-source-transfer-v001 在完成24个 Chronos 分组模型之后，于 TimesFM 旧基线重放检查停止。标准化评分最大差异2.34167441e-9，超过原预设 rtol=atol=1e-10。尚未进入本次新模型的汇总评分。旧源码快照、输入、24个检查点及失败日志全部保留，工作程序23584以非零状态结束，Windows失败通知已提交。

独立诊断 native-reference-batching-v001 使用完全相同的输入特征、候选轨迹和冻结源模型，比较原 R6 批次排列与拼接后的扩展训练批次。原批次对原缓存的预测最大差异在两个模型上均为零。拼接批次对同一缓存的差异为 Chronos 7.0847e-10、TimesFM 5.3312e-8。原始特征和候选轨迹逐元素完全相同，因此该次差异来自相同门控推断在不同批次排列下的浮点计算，不是新的预测器调用、模型拟合或数据变更。

v002 对旧源门控直接复用已审查的原生集合预测缓存，以保持原基线的完整数值。当前扩展门控的训练输入、观测标签、损失、归一化、固定组合求解、种子和训练程序均保持原定义。没有放宽比较阈值。v002使用新的结果目录，并重新拟合；其独立审查必须验证先前24个 Chronos 模型的全部状态张量完全相同，且两版的训练输入与组划分完全相同。此修复不构成任何准确度改进，实验效果仍须等待完整审查。
