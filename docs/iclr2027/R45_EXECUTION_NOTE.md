# R45 执行记录

2026年9月17日，4项专项检查通过，覆盖严格的预测起点边界、人工隐藏保护、原观测不变、两种输入单位、原短输入精确恢复及原量纲输出的统一计分转换。已核对当前模型配置和加载后model_context_length均为8192，没有使用旧封装器的96点规格。

16个未评分小批的80次查询逐值重放，16个原192点查询精确恢复，2664个旧输出全部保持。北京样例实际上下文8192，HDB样例为381—577个已到达时点；模型编码补丁数、原生NaN掩码、时间字段及未知未来字段均核验。没有计算小批的新预测误差，也未修改模型后端、参数或容差。

预检中北京原变量范围的长查询中位耗时约0.84秒，仅目标约0.12秒；HDB约0.026—0.028秒。查询记录的CUDA最大已分配量约0.79 GiB，包含本进程模型及激活，并非增量显存或完整部署内存保证。正式每个查询继续保存实际长度、行数、输入单位、耗时、已分配及预留峰值；不得根据这些预检误差选择长度，因为没有读取精度。

正式仍为314任务，每项4个新长原生查询与1个短恢复，共1570次前向，审计同量重放；4个新输出各配同VAR控制。北京238、HDB111方法，总64191窗口/119169目标分数。新原量纲查询的输出转换到原训练前缀标准单位再评分；缓存沿用context_z字段名，实际单位必须以input_units记录为准，不能从旧字段名推断单位。

两来源和全部六面板保留，HDB后28天未评分。本轮补充更充分历史下的原生基线，长历史有额外信息和计算，不能混称同预算方法对比。尚未建立新的方法贡献或其他预测器的充分比较。
