# R44 执行记录

2026年9月17日，3项专项检查通过，覆盖只改变候选组号、保留原目标NaN及变量范围、候选/目标读出索引、完整目标仍执行新诊断。16个未评分小批任务包含原R43的一个完整目标回退样本。本轮64次固定查询精确重放，144次逐候选串行查询精确重放；串行与分组批量的最大原分位差为1.14e-5，满足事前规定的rtol/atol=2e-5。没有修改数值后端或容差。

15个非回退小批任务的原联合候选查询逐值复现；2056个原输出保持不变。隔离/联合两种候选查询的输入矩阵、内部统计和编码字段相同，只改变组号；目标锚点输入前段的统计/编码也精确匹配目标单独查询。HDB目标单独预测复现原native_target192。全部检查均未计算小批新准确度。

正式314任务各4次查询，共1256次模型前向；审计重放同量。隔离候选的一次批前向包含12或6个逻辑预测组，完整成本不会按一个预测组叙述。每个候选单独读出与既定输出汇总均保留，并补同VAR控制；最终北京230、HDB103方法，共61679窗口/114809目标分数。两来源和全部六面板均保留。

本轮是对R43辅助结果的变量范围及交互诊断，不恢复其失败主方法。新增联合控制不采用旧完整目标回退，旧回退结果另保留，避免比较中混入定义差异。HDB后28天未评分，当前也未补齐所有预测器与更长原生上下文，不能据此认定新方法贡献或投稿就绪。
