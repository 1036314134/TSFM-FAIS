# R42 执行记录

2026年9月17日，正式训练前4项专项检查通过，覆盖零适配恒等、原参数隔离、钩子异常清理、未来NaN标签梯度隔离及相同初始化。真实北京训练前缀上，普通及缺失扰动两种输入的一步更新均具有有限梯度；零适配和移除适配器的原预测差为0。实际目标为97个投影，新增1,206,912个参数，原模型参数摘要保持一致。两次源诊断更新耗时约0.21秒与0.13秒，仅用于确认实现和估算成本。

北京源池864个任务对应96个历史；HDB源池972个任务对应18车场的108个历史。完整候选池共1836项。正式顺序为每来源864项交替，总1728次更新；HDB按固定排列取前864项，无需循环重复。原始历史会因9种扰动多次出现，不能把1836项视作独立历史。两套模型的输入人工缺失不同，预测标签、顺序、样本权重和训练预算相同。

未评分小批各训练12步，核验全部1836个源文件。两套模型均从倒数第二步参数和优化器状态精确复现最后一步及最终参数；源smooth L1还用独立浮点参考核对。15个评价任务的30次适配器查询、15次普通恢复和15次零适配检查逐值一致，原骨干摘要不变。没有依据小批评价准确度选择学习率、步数或模型版本。

正式六阶段为准备、普通适配训练、缺失扰动适配训练、冻结评价预测、评分及独立审计。两组各1728步，共3456次训练前向及对应反向；评价628次适配器查询和314次普通恢复，审计重复这942次并额外执行两套末步来源检查共4次前向和2次反向更新。训练完整日志保存，独立优化核验针对最后一步，不声称逐步重放全部训练。

北京仍为231任务、166个输出，HDB为83任务、51个输出，共42579窗口及80925目标分数。主corruption_lora保持其身份，与natural_lora和所有旧强控制比较；两组相同VAR组合仅为匹配控制。HDB后28天保持未评分。此阶段属于已有适配方法的能力对照，不能只凭LoRA或人工缺失训练认定新增论文贡献。
