# R30：正式评分前的固定组合对照补充

在任何R30未来误差读出之前登记。保留原25方法，并将既有全源/匹配源的最佳单候选、MAE固定凸组合、联合固定凸组合分别迁移到完整peer池和仅目标填补池，增加12个不需要新模型调用的控制。权重与单候选索引复用R19 source_defaults.json和R24 matched_controls.json，不按本轮未来重新拟合。

最终为37方法、231任务、8547窗口评分、17094目标评分。预测调用预算和拟合预算不变。组合名称明确为迁移的源固定控制，原权重来自不同的源人口和L96场景，不称为在新peer任务上重新求得的最优权重。主方法还必须超过这些可部署固定组合，才能声明强对照增量。

准备阶段已完成的无误差小批仍只用于输入/拟合接口验证，原快照保留。该补充只增加固定输出计算，不改变源、人口、peer选择、填补、主方法或阈值。
