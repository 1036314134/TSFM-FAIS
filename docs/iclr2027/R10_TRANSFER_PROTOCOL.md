# R10固定模型的短迁移评估

2026年9月15日，在R10源端独立核验完成后登记。复用R10四条件各三种子、两预测器共24个完整源模型，以及R7点特征teacher/future共12个参照模型。源训练人口、模型参数、目标与训练日程不再调整，不新增填补器拟合或时序预测器调用。

评价原R6全部1,522输入的H96/H192，以及旧373窗口H96，保留原人工缺失、原始缺失、完整上下文等全部面板。目标集合均已使用，只作开发判断。采用已审查33维点特征补64零值与原候选顺序，核对源目标家族及历史分离。四个新方法使用metric_前缀；metric_joint_future仍对应预定主条件。

四种R10程序、两种R7参照各保留三种子与平均预测；另包含R10四种同损失固定组合与固定单候选、R7两种固定组合与单候选、七候选中位数，总37个直接重放方法。原R9迁移结果和更早强基线全部保留；共享参照须复现，不重复添加。预测先保存，之后在真实未来的原观测位置计算训练前缀标准化的精确MAE/MSE。权重由模块与独立网络重放精确比较，预测由独立加权求和核对，指标双路径复算。

这是一项复用缓存的短评估，完成后立即分析，不等下一小时。只有随后启动的正式实验采用小时完成检查。没有预先声称某个条件有效，也不按单个有利来源或预测器选择报告。
