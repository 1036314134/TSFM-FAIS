# 原源开发分位数完整性检查

2026-09-14。检查全部7,812个原开发输入、两个预测器、六个填补候选和原生回退候选，使用原已登记的0.1、0.5、0.9三个分位数。检查仅读取过去输入、预测、元数据和原训练前缀统计，不读取真实未来数组、不计算新预测精度、不拟合或查询预测器。

逐文件核对原始输入、原预测、Chronos修正预测与TimesFM原生缺失缓存的哈希和来源绑定。Chronos的1,512个已修正输入必须采用修正文件；不能恢复或猜测缺失分位数。各候选分位数统一到候选、H96、两个目标、三个分位数的明确轴序。Chronos点输出须与0.5分位数完全一致；TimesFM保留其原生点输出，只记录它与0.5分位数的实际差异。

原生缺失回退只依据历史观测性决定，点输出和三个分位数须一起取自同一个候选输入：Chronos任意空通道时两个目标均取LOCF，TimesFM仅为空目标取LOCF。所有七候选的点输出重新按原前缀均值和尺度标准化，须与accuracy-development-v002逐元素一致。原尺度在常数通道的处理不变。

保存标准化分位数，以及按目标和联合目标计算的平均区间宽度。宽度使用原始上、下分位数之差除以原前缀尺度，保持既有response.interval_width的定义与范围限制。统计非有限值、分位数交叉和上下端点倒置，不排序或截断分位数。质量标记用于判断后续输入是否可用，不能解释为预测精度或概率校准保证。

输出为source-quantile-audit-v001。先通过原生回退、目标尺度、分位数交叉和非有限值的四项针对性检查，再通过原有守护队列执行完整核对。仅用单核Idle，继续第三优先级和180秒空闲准入。此检查不授权或登记新的模型训练、原始缺失概率预测收集或概率组合规则；下一项是否值得实施由完整结果决定。
