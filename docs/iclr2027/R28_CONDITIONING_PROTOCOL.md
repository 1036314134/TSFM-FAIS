# R28：近期观测条件化的完整开发筛查

2026年9月16日，用户明确同意按PRO_CONDITIONING_REVIEW_20260916.md继续。正式预测前固定本协议。材料角色为开发实验，不是新确认。只有训练前缀标准化后的真实未来MAE/MSE用于主要判断，填补误差不参与选择。

人口固定R25的301个历史、9数据集、8来源组；Chronos-2，原变量集合、前两个目标、L192/H96、近期区间B96。沿用原前缀均值/总体标准差、未来原观测评分及来源等权。保留R26的19个旧方法，不重训任何模型或填补器，不读取其他站点，不启用未认证储备。该应用条件不依赖R27待定的跨序列信息许可。

主方法conditioned_repair：较早96步为历史；近期96步原观测作为原生future_covariates，缺失仍为NaN；读原生0.5分位，仅回填近期原缺失，然后以192步历史预测96步。原观测逐值不变，较早缺失仍为NaN，补入值在第二次调用中可见。只读原历史，真正未来输入由程序新建为全NaN，不接受当前真实未来数组。

五个新增过程控制为unconditioned_repair（第一步不提供近期条件）、direct_conditioned（一次查询192步取后96步）、native_single_rollout（相同第二阶段分位预测按原生单路径规则汇总）、native_multi_rollout（固定原生九路径0.1至0.9）、budget_native192（原L192输入输出192步取前96步）。原生单路径直接骨干0.5输出与主方法的重合是已知性质，不将这一重复声称为新方法。

七种原填补值只替换近期缺失、较早NaN保持，逐一预测；加入旧原生构成同区域八候选，保留八单候选、预测均值/中位数、原全源及匹配源的三种固定策略，共16个recent_控制。权重完全复用原源拟合，不根据当前未来重拟合。总计19旧方法+6新过程+16同区域方法=41方法，12341个窗口评分、24682个目标评分。

已清查的1个MetroPT历史存在较早目标全缺失，六种条件/跨度过程中的五种条件过程整体回退旧guarded_direct，budget_native192正常执行。若近期完整，五种条件过程同样回退原生；当前301人口无此类历史。条件预测产生必需位置非有限值时，五种条件过程共同回退原生并保留原因及全部已生成查询；程序错误直接停止，不能当数值回退隐藏。同区域候选或跨度控制若所需输出非有限，单独回退原生。原始301历史均保留，禁止删除失败样例。当前没有辅助变量独有的较早全缺失或完整历史全缺失通道。

原生九路径由已保存第一阶段分位预测构造，路径间group_id分开，原通道在同路径内共享group_id；已知近期值回写后再预测。记录实际多路径输入与原始分位输出。单路径和九路径均采用已安装Chronos的概率质量/加权分位规则，原始0.5读出另列，不将两者混称。

先运行按输入元数据固定的9个小批历史（8来源各1例，加1个回退例），不读新预测误差；核对原生接口、旧缓存、同区域输入保护及原生九路径接口。正式冻结所有代码、协议、旧输入/预测与权重摘要。正式预测全部完成后才统一评分，随后独立重建输入、分位汇总、原观测未来损失和来源聚合。

数值门槛预先固定：相同原始查询重放要求逐值一致；公开/私有单次预测按前缀标准差折算atol=1e-6；不同分位汇总实现的独立重建采用rtol=1e-5、atol=1e-6，主/原生单路径骨干输出要求逐值一致；误差独立重建atol=1e-12、rtol=1e-12。容差只用于数值检查，不是改善判据。

完整预算至多4214次新逻辑模型前向，包含九路径批调用；不同路径批不能视为同成本。缓存、重复输入和回退可减少实际查询。记录唯一原始查询、逻辑调用、缓存、实际模型forward、路径加权变量数、GPU峰值和耗时。独立核验的全查询重放与前置小批另外计数。阶段超时每项2小时；第一优先级，但不停止或修改其他项目进程/数据/环境。

主资源判据：conditioned_repair对原mean8/median8的MAE均至少降低1%，且优于recent均值/中位数、源固定组合和guarded_direct；同时检验相对unconditioned的增量。1%不是统计显著性或ICLR标准。若被强控制在MAE/MSE同时支配，则停止该固定方案。所有MSE取舍完整报告。若某个预设控制有正信号，保留其原控制身份，另行登记后续问题，不重命名主方法。

报告全部8来源及8种留一来源敏感性，不通过删来源恢复成功。按输入预定的近期目标缺失率(0,0.1]、(0.1,0.3]、(0.3,1]，目标末端缺失与内部缺失、同期辅助观测可用率和回退描述分层；分层仅用于诊断。全人口失败后不自动添加B、种子、阈值或新网络。

正式实验期间每30分钟只检查一次状态；仍运行时结束检查，不分析局部结果、不额外编码。短诊断和失败定位即时处理。全部预测、读出及独立核验完成后才解释结果。自动推进沿用9月17日晚内部停止日期及完成/失败/关键变化通知偏好。
