# R29：未知查询与已知条件的输入角色诊断

2026年9月16日，在R28完整核验后登记。R28主方法0.585719/0.836456未胜mean8或recent_mean8，固定两阶段路线停止。相对无近期条件控制，近期条件使7/8来源MAE改善；direct_conditioned则明显退化。这些结果支持核查输入角色，尚不证明角色混合是失败原因。

官方公开数据构造将预测目标的future_covariates部分填为NaN，再拼接已知协变量；R28从部分已知条件行读取缺口输出。模型损失实现按单元掩码排除已知条件，因此不能声称R28属于非法调用或已证实未被训练覆盖。本轮只测试更贴近公开目标/条件区分的固定表示，不改变历史长度、B、模型权重、训练目标或阈值，不训练新模块。

信息仍为原301历史、原变量、Chronos-2、L192/H96/B96、两个目标、原训练前缀标准化及8来源等权。将较早A历史复制成两组相同行：第一组为查询目标Q，第二组为条件副本C。Q的整个第一次预测区间保持未知；C仅包含近期R已观测值，缺失位置仍NaN。原变量在同一episode内共享group_id；没有引入新测量、其他站点或真实未来。输入行数由D增加为2D，最大80行，计算增加单独记录。

主query_role_repair读Q行0.5输出回填R原缺口，第二次使用原D变量的192历史标准预测。helper_role_repair读同一次调用C行0.5输出执行相同回填，是相同信息、相同复制数、相同调用数的匹配控制。duplicate_unconditioned_repair的第一次两组行均不给R已观测值，检验增加相同通道是否足以解释结果。query_role_direct按Q/C角色一次输出192步，从Q取后96步，作为预设过程控制。四个新方法加R28全部41方法，共45方法、13545窗口/27090目标评分。

1个较早评分目标全缺失的MetroPT历史使四个新方法共同回退原guarded_direct，所有窗口保留。无近期缺失时同样回退，当前人口无此情况。需要回填或评分的输出非有限时四个方法共同回退并记录见证；代码错误停止执行。没有事后删源、选择窗口、选择预测器或更名主方法。

源输入、调用和输出先冻结后统一读当前未来。无误差小批沿用8来源加1回退例，核对Q未来全未知、C真实未来全未知、公开SDK与原始调用预测一致、原观测保留与较早NaN不变。正式每例至多6次前向，共1806次上限，重放和前置诊断另计。共享首阶段只计一次，两个后阶段单列。保留骨干与代码摘要、完整输入、分位输出和查询身份。

数值门槛沿用R28：同输入同原始调用逐值重放，SDK路径前缀标准差折算atol=1e-6；误差和层次聚合rtol/atol=1e-12。比较不含分位混合的新方法，缺口和最终预测均固定原生0.5分位。不改变容差以通过检查。

首要资源筛查仍是MAE相对原mean8/median8至少1%改善，并胜当前最强recent_mean8/recent_median8及源固定/原生；MSE完整保留，受强控制双指标支配即停止。相对helper_role_repair与duplicate_unconditioned_repair没有增量时不能归因查询角色。控制的正结果保留为控制，不重命名原主方法。全部8来源和8种留一来源结果同时报告，分层只做描述。

这是另行登记的一次输入角色诊断，不代表新颖性或独立确认成立。没有强信号就停止该角色变体，不自动扩展复制数、B、窗口、种子或网络。未用储备仍封存。本轮保持第一优先级；不停止或修改其他项目。预测、评分、核验运行期间每30分钟仅紧凑检查，全部完成后再分析。
