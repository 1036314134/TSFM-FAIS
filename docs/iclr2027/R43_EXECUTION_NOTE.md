# R43 执行记录

2026年9月17日，4项专项检查通过，覆盖原始行及NaN保持、原观测保护、候选/目标读出索引、匹配副本数量和完整目标的共同回退。16个未评分小批包含北京/HDB、两个跨度、三个面板及一个完整目标任务。105次辅助查询、16次普通预测逐值重放，90次原始输入统计和编码字段对照精确一致，1736个旧输出全部保持。没有修改模型参数、归一化实现或容差。

北京的参考池确认为7种原填补、4种原统计修复及静态Gaussian，共12条轨迹/目标；HDB为既有6种简单/统计填补。所有已知目标值精确保持，参考序列按原目标训练前缀单位转换。最大联合行数为北京41、HDB13。所有辅助信息来自旧历史产物，没有提供未来已知字段，也未读取新评价误差。

正式仍为314任务；8个完整目标HDB任务共同回退原生并保留计分，其余306个每项7个干预查询，另有314次普通核验，共2456次完整模型调用。独立审计重放同样数量。10个新点输出各给同VAR控制，原166/51方法分别扩为186/71，总48859窗口、91825目标分数。主raw_plus_pool只读取原始目标行，不因控制胜出切换读出或改变主方法身份。

本轮复用已生成的填补轨迹，尚未重新计量全部上游部署成本；一个基础模型调用不等于整个候选池免费。原始副本控制、单Gaussian/KNN/中位参考、候选联合聚合、原填补预测池、原生长历史及R42适配全部保留。后28天HDB未评分；此次仍是两个已用来源上的开发检验，不能视为独立确认或投稿就绪证明。
