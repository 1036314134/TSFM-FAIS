# 目标级选择的短迁移评估

2026年9月15日，在R11源端独立核验后登记。复用Chronos的target/broadcast各三种子和按目标固定/单候选；TimesFM复用R10 joint_future三个种子。目标范围为原R6全部1,522输入、H96/H192，以及旧373窗口H96，不改变原面板或数据准入。全部目标数据已使用，结果属于开发判断。

按当前预测目标重建可见33维特征，Chronos保留原联合预测器的整体回退语义；broadcast按原上下文复制联合特征。TimesFM在目标侧也必须精确复现原特征，并沿用原决策顺序，避免批次重排带来的数值变化。原预测器候选输出和前缀标准化参数不变。Chronos新增程序11个（含中位数与种子），TimesFM5个（含中位数与种子）；统一主方法名为scope_target，TimesFM这一名称仅为原metric_joint_future的复用别名。

保存全部预测之后才在真实未来原观测位置计分；检查神经权重重放、独立加权预测、broadcast的目标共享权重、精确MAE/MSE双路径与旧中位数。TimesFM各别名在所有面板须复现原对应模型的MAE/MSE。原R10比较表及更早全部基线保留。无新拟合或时序预测器调用，完成后立即分析，不额外等一小时。
