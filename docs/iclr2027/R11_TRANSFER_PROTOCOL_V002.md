# 共享对照的推断次数修正

完整沿用R11_TRANSFER_PROTOCOL.md的模型、目标人口、37式旧参照保留原则、真实MAE/MSE口径和逐目标主方法。首次短评估在Chronos的broadcast对照检查处停止：完全相同的重复输入行在批量浮点推断中产生微小权重差，尚未完成评分。旧程序快照和身份文件保留于target-local-transfer-v001。

六个种子/跨度组合的短诊断已完成，两个目标的输入特征完全相同；重复推断的最大权重差约2.02e-7，改为每个上下文推断一次并复用后，两个目标权重精确相同。预测差最大约1.25e-7，未读取真实未来、未拟合模型或调用时序预测器。记录shared-inference-diagnostic-v001。诊断曾在本地Arrow扩展加载处发生原生崩溃，先加载该扩展后完成；没有修改依赖版本。

本次只将broadcast对照按共享决策定义实现为每个上下文一次小门控推断，再将权重复用到两个目标；仍先验证两行输入完全一致。target主方法、TimesFM、所有固定模型与数据不变，不以放宽一致性阈值处理该问题。新输出为target-local-transfer-v002，仍须完整验证预测、评分及TimesFM复用别名。
