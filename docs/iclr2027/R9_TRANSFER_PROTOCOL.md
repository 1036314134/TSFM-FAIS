# 固定源模型的短迁移核验

2026年9月15日，在R9源端结果读出后登记。本核验复用24个R9完整源检查点，以及R7完整源point_teacher/point_future共12个检查点；没有重新拟合、调参或调用时序预测器。它属于短评估，完成后立即分析，不按正式长实验的小时节奏延迟。

目标范围固定为原R6全量1,522输入、H96与H192，以及旧373窗口、H96；保留人工缺失、原始缺失和完整上下文等原面板。不增加或按误差选择目标历史。两套目标数据均已使用，结果只用于开发判断。所有源模型训练家族和历史必须与目标排除核对。

比较expanded/steps_matched×teacher/future四程序与R7点特征teacher/future两参照，每项保留三种子及平均预测。再加两套源人口各自的teacher/future固定凸组合与固定单候选，以及七候选中位数，共33个直接重放方法。完整旧结果保留，包括原主门控、原生输入、MoTM与八候选中位数。R9主条件继续为expanded_future，不按迁移表现更换。

使用已审查的目标33维点特征并补64零值，原候选顺序保持不变。逐模型核对检查点与人口，采用模块推断和独立网络重放精确核对权重，再用独立加权求和核对预测。所有目标预测先保存，再读取真实未来的原观测位置，按各自训练前缀标准化MAE/MSE评分；指标双路径重算，原七候选中位数必须复现。报告所有面板、家族及种子，不能把训练误差或部分面板的改进充当普遍收益。
