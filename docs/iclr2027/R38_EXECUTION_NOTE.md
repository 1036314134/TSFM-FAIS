# R38 执行记录

2026年9月17日，正式预测前完成3项预测组合数值测试、5项官方数据解析测试，合计8项通过。8个未评分小批任务保留原1120个输出，48个新增组合通过独立创新响应因子与白化求解复算。协方差最大差1.44e-15、预测最大差1.55e-15，均满足预先约定的rtol=1e-9、atol=1e-10。小批没有计算下游误差。一次启动前JSON序列化问题通过将NumPy布尔值转换为Python布尔值修复，不涉及方法或数值变更。

正式实验固定231个开发任务及146个输出，三个阶段为冻结预测、完整评分、独立核验。既有140输出逐值保留；33726个窗口分数和67452个目标分数，以及站点、站点宏平均、逐站排除汇总全部复核。没有新增基础模型调用，也没有新增模型拟合。主方法correlation_transport和自然H24主面板保持预先登记身份，不能根据结果替换。

官方数据采集独立于R38方法评价。日界线查询诊断、失败的第一版四点探针和修正后的第二版均保留。第二版4个小时、1991条序列、7964个单元已经从完整官方JSON独立重建，所有数值和标志一致。其中357个单元更新时间过期，25个单元的C类更新时间异常或晚于网格；这些类别分别保留。正式采集固定56天共1344小时，不执行预测或筛选有利序列。

正式脚本、契约、队列配置、依赖版本及原预测来源在运行前保存摘要和副本。后台各阶段设置7200秒技术超时；数据采集使用独立轻量进程，不调用GPU或修改其他项目。实验队列保留系统完成提醒；半小时自动检查同时查看实验与数据采集状态，任一仍运行时结束本次检查，完成后才分析。
