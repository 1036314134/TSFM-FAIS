# R15核验的数组排列修正

2026年9月15日，R15读出于10:34完成，独立核验随后在TimesFM的independent_ar3历史1、30%随机缺失、七候选、目标0、rho=0.5条件报错。512个未来副本中一个MAE最小候选编号与原记录不同。原读出、见证、协议、原审查脚本及失败日志全部保留。

audit-replay-diagnostic-v001复现同一错误。原核验用整数索引提取未来目标，得到列连续数组；读出用切片得到跨目标步长的视图。两者数值逐值相同，但后续绝对误差平均的浮点求和顺序不同。在副本206上，原读出候选0和2的MAE均为0.9380721578677962，按首个最小值选择0；原审查分别得到0.9380721578677966与0.9380721578677963，选择2。重放原切片顺序后全部512个编号一致。该样例方差和未来样本差均为零。

audit_future_dependence_v2.py保留原审查所有指标、完整协方差、直接梯度和风险重建检查。未来切片及方差缩放的算术顺序与登记读出一致，以逐值重放样本和并列选择；原分析风险仍以另一高斯绝对误差公式核对，投影方差仍以稠密协方差核对。所有原阈值保持不变，不增加并列容差、不修改选优规则或已完成结果。

新核验绑定原审查脚本的登记校验值并另存自身校验值及本说明。输出dependence-audit-v002，检查同一份已完成dependence-v001，无新抽样试验、拟合或预测器调用。该工作属于失败后的短核验修复，即时完成，无需再等待一小时。
