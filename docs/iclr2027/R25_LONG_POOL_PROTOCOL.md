# R25：Chronos正信号的完整长历史基线检查

2026年9月16日登记。R24主pattern假设未成立；其Chronos fraction控制在L192的23个旧开发历史上出现MAE约4%、MSE约16%的改善。本轮将该控制明确登记为新的候选方法fraction_repair，冻结原seed5101权重，检验其优势能否在更完整的基线和更大开发面板保留，不改写R24结论。

固定已有R5/R6原生任务中，原96步目标历史存在缺失的全部301个历史，准入规则与R21相同，不按当前误差筛选；8来源组，两个目标，Chronos联合原全部变量，评价跨度96。本轮历史长度192，原时刻、原观测掩码、原预测目标和训练前缀标准化保留。先固定事件表，再生成新预测。23个R24旧任务作为重放核对，新增覆盖仍来自已用开发数据，不称独立确认。

八候选为原LOCF、线性插值、季节填补、多变量KNN、SAITS、TimeMixer++、MoTM及原生回退。两个深度填补器在原数据集拟合序列的原20%前缀上重新拟合L192模型，最多64个训练窗口、步幅24、10轮、batch16、模型种子0；沿用六缺失机制、训练缺失率0.1/0.2/0.3/0.4/0.5和掩码种子1101/1102/1103。训练窗口长度改变，原有L96神经产物不能直接当作L192对照；所有新拟合和失败单独记录。拟合窗口必须在各自原前缀内，且最新拟合日期不得晚于相应当前预测起点。失败或部分输出沿用原LOCF/前缀回退，并保留覆盖率，不能隐去失败后声称完整强基线比较。

传统统计按同一前缀重建，季节相位按实际历史起点对齐。MoTM使用原冻结网络、ridge0.5和batch32，允许192步网格上的上下文优化，计入成本。所有原观测值逐值保护。fraction和原pattern模块继续使用同一季节填补192输入，参数不更新，表示修复规则不改。fraction检查点SHA cb8f7d7e0c2a8f2a5dbcfe25a4cfa107aa8d5a9f0d2d53c31e3477ff5958b06e。

报告八个单候选、预测均值/中位数、原全源MAE单候选/MAE固定/联合固定三个控制、R24匹配源样本的三个固定控制、fraction_repair和原pattern_repair，共18方法、5418窗口/10836目标评分。源固定组合权重从已有L96源任务迁移，明确记录该信息，不用当前自然未来拟合新的组合。主fraction对median8、最佳可部署源固定方法和原生处理比较，MSE完整保留。按序列、数据集、来源组逐层等权，新加坡两数据合并。

全部填补及预测先冻结，随后才评分。独立核验原始输入和尺度、拟合边界、基线产物、MoTM冻结、全部原观测保护、R24已有23任务重放、当前模型参数及观测未来损失。首次小批只核对L192拟合、输入和吞吐，不看预测误差。完整结果若不再超过同长度中位数/强固定方法，停止扩大该正信号；若保留才登记多种子和更严格机制对照。TimesFM的负结果继续保留，本轮不将单模型进展写成普适结论。

第一优先级，四阶段准备/预测/读出/独立核验；正式运行期间每30分钟只检查一次，未完成立即静默结束。新储备来源暂不参加试错，原始数据、其他项目资源和既有产物保持其原状态。
