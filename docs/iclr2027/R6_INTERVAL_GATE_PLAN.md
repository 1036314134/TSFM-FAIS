# 区间输入的匹配源端对照

2026-09-14，在全部源分位数核对完成后登记。两个预测器的分位数与七候选点输出完全对齐且均为有限值。Chronos有536个分位数交叉位置、其中1个上下端点倒置，记录保留；不重排分位数，不按这些标记删除输入。宽度采用现有response.interval_width定义，是模型报告的平均端点差，不承诺校准正确或误差较小。

本次只恢复原33维中的response.has_quantiles与response.interval_width，前者为1，后者分别使用Chronos的联合目标平均宽度和TimesFM对应目标的平均宽度。其余31维、七候选预测、源完整历史教师损失、家族权重、网络容量、25轮训练与三个种子均不变。两个字段的恢复不构成一种新的概率组合方法，也不把置信区间质量当主要指标。

仍用原15家族、165训练历史；按原训练/验证划分及整家族留出做30折、三个种子，共90模型，再拟合6个完整源模型。训练前先分别重现两个预测器原point-only完整源门控的seed5101，并逐状态张量比较。新模型平均三个预定种子的权重。保存全部新预测后，读取原验证真实未来计算训练前缀标准化MAE/MSE；比较原point-only门控、原固定凸组合、单一源固定候选、均值和中位数。旧控制直接复用其原始预测缓存，保持数值与批次定义。

独立审查验证原31维完全相同、区间字段正确对应目标和输入、拟合统计仅来自当折训练数据、96个检查点和预测重放以及真实未来评分。分位数交叉不作隐含修正或事后剔除，不挑选种子或家族。此对照检验新增可见区间信息的作用，不能据一次源验证就宣称它能迁移。

本次无新预测器调用、填补拟合或原始缺失评价。仅当源端结果足以支持继续时，再另行登记所需的原始缺失分位数收集及固定模型评价。模型主要指标仍为下游MAE/MSE，继续第三优先级、单核Idle和结束事件提醒。
