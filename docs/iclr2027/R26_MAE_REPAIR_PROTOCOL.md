# R26：修复模块训练指标对齐检查

2026年9月16日，R25完整基线检查未保留MAE优势后登记。仅检验一个明确变化：将Chronos fraction修复的源预测训练损失改为平滑绝对误差，保持低秩模块、缺失块限制、初始化、数据、优化顺序、学习率、梯度上限、轮数和表示能量正则不变。该检查不为原联合损失方案增加种子或阈值网格，不改写R24/R25负面结论。

源任务仍为149历史、2682个L96输入、14来源；验证864输入，electricity按64维上限排除。固定seed5101、rank8、3轮8046步、AdamW学习率/权重衰减1e-3、梯度上限1。新预测项为来源平衡的mean(sqrt(e^2+1e-6))，e按原训练前缀标准差表示；另加与R24完全相同的1e-3表示相对能量正则。只训练一个Chronos fraction模块，基础模型参数冻结。末轮固定评价，不早停、不挑检查点。

以同初始化的R24联合损失fraction为直接控制，同时保留其pattern和全部既有强固定基线。源验证L96在原19方法上新增mae_repair，共20方法；原生开发301历史L192在R25原18方法上新增mae_repair，共19方法。合计22999窗口/45998目标评分。主比较为原生L192上的mae_repair对median8、mean8、匹配源MAE固定组合及原生处理，MSE完整报告。只有相对旧联合修复改善、但仍落后强固定对照，不算通过。

模型输出全部冻结后读取评价未来；不使用当前未来拟合权重或选择超参数。评价、观测掩码、训练前缀尺度和8来源等权汇总沿用已核验记录。只在MAE改善且没有由少数来源掩盖明显风险时考虑后续投入。新储备不参与试错，单模型单种子结果仍不是独立确认或投稿质量证据。

训练前检查源样本梯度、同初始化和更新后公开推断；审查最后一步优化器重放、未变化骨干、全部新预测重放及独立误差。数值容差沿用R24，修复模块公式按实际float32计算。正式训练/预测/读出/核验运行期间每30分钟仅检查一次状态，完成后再分析；短诊断与失败修复即时处理。
