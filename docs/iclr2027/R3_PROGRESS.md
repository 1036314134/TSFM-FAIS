# R3 工作记录

## 2026-09-10：协议修正与首轮开发实验

用户已授权按预测收益估计与可靠序列级选择方向推进实现、实验和论文修订。初始完整首轮工期估计为 8—12 个工作日，后续根据实验吞吐量和方法证据调整。

已完成：共享训练前缀 MASE 尺度、教师显式尺度接口、Chronos-2 原生 NaN 输入入口、序列级部署特征和预测响应特征、相对收益回归、开发集切换阈值选择、同监督静态特征对照、重建监督对照、预测均值和中位数集成、家族留出开发流程、版本化缓存及完整性检查。旧预测入口仍要求有限值，R2 教师默认保留原口径以供复现。

验证：512 项现有及新增单元测试通过。出现一条既有 MICE 收敛提示。受影响代码通过静态检查。新端到端测试覆盖候选缓存、原生缺失输入、预测缓存复用、家族隔离和缓存篡改拒绝。

首轮配置：`configs/iclr27-r3/development_pilot.yaml`。输出：`artifacts/iclr27-r3/development-pilot-v001`。共 192 个窗口任务，126 个训练任务、66 个验证任务；验证仅包含 11 个独立时间窗口，覆盖 6 个家族。六种填补流程均运行，LOCF 和线性插值各有两个部分有效任务，保留完整回退流程及原始状态记录。

| 模型 | 响应收益选择 | 校准后选择 | 静态特征预测监督选择 | 原生缺失 | 预测中位数集成 |
|---|---:|---:|---:|---:|---:|
| Chronos-2 | 2.438163 | 2.436895 | 2.404717 | 2.395580 | 2.386990 |
| TimesFM 2.5 | 1.948867 | 1.946892 | 1.988203 | 默认缺失处理待补测 | 1.691328 |

数值为家族等权 MASE，越低越好。TimesFM 上的 LOCF 为 1.581038，重建监督对照为 1.565594，均优于响应收益选择。当前证据不支持把响应特征或阈值规则写成已经验证的优势。

候选准备耗时约 150 秒，包括首次深度库加载；Chronos-2 和 TimesFM 的预测阶段分别约 23 秒、27 秒。该值来自紧凑开发数据，包含 clean 诊断调用，不等于在线部署成本，不能直接外推到高维全量实验。

扩大实验配置为 `configs/iclr27-r3/development_expanded.yaml`。保持候选池和方法不变，将独立训练窗口上限提高到每条序列 16 个、验证窗口 4 个，覆盖原有 18 个家族、6 种缺失机制、3 档缺失率、2 个种子。短序列是否满足分区条件会在清单中显式记录。该轮仍属于开发实验，不作为最终确认数据。

尚未完成：收益特征的稳定增量证据、历史回测强基线、严格在线成本比较、未见预测器迁移、新数据上的独立确认，以及依据有效结果重写论文。现有英文及中文论文尚未改写为正结果叙述。

## 后续确认数据的结构性调查

已检查 R2 未纳入的 9 个家族的原始观测掩码，5 个家族存在满足完整未来标签要求的候选窗口。计数及适用限制记录在 `R3_CONFIRMATION_PROTOCOL_DRAFT.md`，机器可读记录为 `artifacts/iclr27-r3/confirmation-source-audit-v001.json`。尚未生成这些家族的预测结果。

## 同日：基准、训练目标与历史反馈对照

首轮开发结果显示，训练集最佳固定候选不一定是新家族上的可靠回退动作。以 LOCF 或 Chronos-2 原生缺失处理为预先指定参考，并同步调整响应特征的参考预测后，继续比较 L1 与 L2 收益回归。L1 拟合条件中位数；L2 拟合条件均值，更直接对应平均损失差的估计目标。

固定参考 L2 开发结果中，Chronos-2 相对原生缺失参考的响应选择 MASE 为 2.380525，原生缺失为 2.395580，中位数集成为 2.386990；TimesFM 相对 LOCF 参考的响应选择为 1.592344，校准后为 1.567506，LOCF 为 1.581038。不同模型和校准设置下的增益不一致，当前不能视为稳定的机制证据。

观测历史回测仅使用此前已结束预测区间内原本可见的值。首个窗口使用预先指定的参考动作；后续使用最近一次或累计历史平均误差选择。试跑中 45.45% 的任务拥有可用反馈。TimesFM 上的回测 MASE 为 1.553532，优于上述收益估计；Chronos-2 从原生缺失动作开始的回测为 2.428898，差于始终使用原生缺失。该对照需要每个历史服务窗口生成全部候选预测，成本不能按一次固定候选调用计算。

进一步比较有界收益目标：以候选与参考预测的平均尺度化距离归一化损失差，拟合取值范围在 [-1,1] 的条件目标，再乘回已知距离。该界来自绝对值三角不等式，不构成选择准确率或分布外风险保证。试跑中该方式对 Chronos-2 原生缺失参考有改善，但对 LOCF 参考及 TimesFM 明显退化，需完整保留并在扩大开发实验中核验。

除已通过的 512 项单元测试外，历史反馈新增 5 项检查、有界目标新增 2 项检查通过。历史反馈验证包括可见单元筛选、目标等权、最低观测量、异常预测拒绝；源数据和预测缓存重新核验后，试跑回测结果保持不变。

## 同日：扩大开发实验

候选准备已完成，共 7,812 个任务：5,940 个训练任务对应 165 个独立时间窗口，1,872 个验证任务对应 52 个独立时间窗口。实际覆盖 15 个家族。JOLTS、US Labor 和 Job Claims 因缺少满足时间隔离的训练与验证区间被显式跳过。每个窗口包含 6 种机制、3 档缺失率和 2 个掩码种子；这些重复掩码不计为独立窗口。

所有纳入序列的训练前缀 MASE 尺度均为正，未触及 1e-8 下限。候选准备耗时 1,376.5 秒，主要高维成本来自 321 变量的 electricity。预测及扩展对照运行中，尚未产生本轮最终汇总。

## 同日：TimesFM 默认缺失处理更正

进一步检查已安装的 TimesFM 2.0.0 包发现，2.5 模型的官方预测接口会去除连续开头 NaN，并对剩余 NaN 做线性插值，随后使用填充掩码补齐输入。此前将直接缺失输入对照记为不适用并不准确。它与 Chronos-2 通过缺失观测掩码推理的实现不同，但同样是必须评价的默认处理流程。

独立基线入口为 `scripts/evaluate_timesfm_vendor_missing.py`，保留当前六候选开发实验及其源代码快照，额外记录默认处理结果与模型库实现哈希。扩大实验完成后会串行执行参考动作/目标变换、观测历史、跨预测器迁移及 TimesFM 默认处理对照。完整后续入口为 `run_utility_experiment.py followups`。

该默认流程已在首轮全部 192 个任务上完成真实模型验证，验证集家族平均 MASE 为 1.562969，优于 LOCF 的 1.581038 和固定 LOCF 参考的 L2 校准选择 1.567506。其结果保存于 `development-pilot-v001/timesfm-vendor-missing-v001`。新增两项检查覆盖官方预处理语义，以及适配器将原始 NaN 正确交给官方接口的行为；均通过。原有 512 项单元测试及本次 9 项后续对照检查通过，新增代码静态检查通过。

两个开发预测器之间的迁移试跑也已完成：Chronos-2 监督训练的 L2 响应选择在 TimesFM 上为 1.548675，反向迁移在 Chronos-2 上为 2.425145。每个折排除评价家族的所有监督，只使用源预测器的标签和校准数据。共有候选池限制为六种有限值填补流程；Chronos-2 原生缺失固定基线仍更强。因此该结果既不能作为两个方向均优越的证据，也不能当作从未检查过的新预测器确认。
