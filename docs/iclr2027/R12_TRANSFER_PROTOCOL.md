# 同八候选池的短迁移比较

2026年9月15日，在R12源审查完成后登记。复用每预测器三个完整源八候选模型和同池固定控制，在原R6全量H96/H192与旧373窗口H96评估。保持原目标面板、历史准入与训练前缀标准化；全部目标已使用，只作开发判断。

直接读取既有目标MoTM填补和预测，核对其发布代码、模型与运行配置来源和源端一致；不重新拟合MoTM局部编码，不新增时序预测器调用。按原联合/逐目标决策单位重建八候选33维特征。八候选中位数与MoTM单独输出必须精确复现其既有预测，并在原评分口径下复现原基线。

主pool8_joint_future及三种子、同池固定和单候选、均值、中位数、MoTM、原七候选中位数共10个直接重放方法。保存所有预测后才读取未来原观测位置，独立核对神经推断、加权预测、MAE/MSE及旧比较；原R11及更早比较完整保留。短评估完成后立即分析，无额外小时等待，不据单个正面面板更换主条件或宣称新的独立确认。
