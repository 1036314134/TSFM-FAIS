# 含MoTM的候选池匹配试验

2026年9月15日，在R11迁移结果后登记。保持R10的决策单位：Chronos每个联合预测上下文一组权重，TimesFM每个目标一组权重；不继续采用未稳定迁移的Chronos目标级变体。原165训练/52验证历史、15家族、单种子6101的3,906输入、L=H=96、标准化与全部分割不变。

在原六个填补器及guarded_direct之外加入motm_reference，共八候选。MoTM使用已核验的官方b406660be4e9e147c3622b4e2f23c71789c26811代码与三个发布模型，ridge0.5、batch32、局部复制种子42，原三个模型均用3次上下文内优化。只拟合当前可观测历史的局部编码与ridge，预训练网络参数冻结；不使用真实缺失值或预测未来。保留该参考中Solar训练组件的来源限制，相关太阳能结果不能证明预训练外泛化。

先重放原三个固定源可行性样例，使用原官方比较容差rtol/atol2e-5，并要求原观测精确不变。为全部3,906上下文准备MoTM结果，按episode缓存与恢复，记录局部拟合变量数、整列回退、用时及原生覆盖率。全缺失列沿用原LOCF/前缀回退；覆盖率只计MoTM实际可拟合列的缺失位置。准备不读取clean_context或future。新计算属于局部上下文拟合，不称为零计算成本。

仅补充MoTM的Chronos/TimesFM预测，原七候选直接复用R7同调用方式缓存。相同有效输入允许复用，首个及每128个新查询重复检查，冻结预测器参数前后核对。主报告同时记新增填补与预测调用成本。八候选特征仍为同一33字段补64零，pool_distance等字段自然随有限候选池变化；一般化输入函数在七候选时须精确重放原特征与预测向量。

学习器使用SharedForecastGate的八候选版本，隐藏16、输入97，每种子2,121参数（相对七候选仅多一个动作偏置）、25轮、128批量、AdamW学习率/权重衰减0.001、梯度上限1、种子5101/5102/5103。目标仍为实际训练未来的MSE与平滑MAE各半，epsilon0.001，不搜索比例。15家族留出及完整源拟合、两预测器共96新模型，主pool8_joint_future。作为实现核验，通用训练函数在七候选时重训每预测器完整源seed5101，与原R10对应模型参数和训练历史精确比较。

提供同八候选、同训练标签的固定联合损失组合与单候选，以及八候选均值、中位数、MoTM单独输入；与原R10全部46方法并列。新增9个输出（含三种子），总55方法、154,440决策评分。全部外层预测冻结后才读取验证真实未来，评价精确训练前缀标准化MAE/MSE；不以填补误差或平滑训练目标替代。

正式阶段为MoTM准备、补充预测与输入整理、训练、独立审查，第一优先级、单核管理；各阶段上限3/3/3/2小时。审查核对来源与参数、上下文边界、原观测、回退与覆盖、有效输入/预测映射、七候选接口和两原模型复现、96新模型人口/统计/预算、固定最优性及评分。运行期间每小时检查一次；完成后立即分析。候选增多和常见组合网络本身不构成新颖性，已用验证与目标集合仍不称新独立确认。
